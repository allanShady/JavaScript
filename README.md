## Playground samples in JavasCript and Typescrip

Each repository directory represente a specific sample and its solution. 

```typescript
  interface Person {
    firstName: string,
    lastName: string
  }
  
  console.log('Welcome 🧘🏿‍♂️🙏🏿', '${Person.firstName} ${Person.lastName}');
```
